// bin_lcr_p1_kit — the LCR-P1's leads, its PCB adapter, and the odds that came
// with it. A small handful, so a plain 2x2.
//
// PRINT: as emitted, foot down. No supports. x1.
include <instrument_holders_common.scad>

bin(2, 2, H_LCR_KIT);
