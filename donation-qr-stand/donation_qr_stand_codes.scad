// donation_qr_stand_codes — dark QR inlay (modules + labels).
// Print FLAT in the dark colour. Load at the same origin as the body; the inlay
// drops into the body pockets and tops out flush with the light background.
include <donation_qr_stand_common.scad>
faceplate_codes();
